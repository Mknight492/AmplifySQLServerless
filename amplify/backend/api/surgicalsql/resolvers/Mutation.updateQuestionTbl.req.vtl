#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateQuestionTblInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateQuestionTblInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE QuestionTbl SET $update WHERE id=$ctx.args.updateQuestionTblInput.id", "SELECT * FROM QuestionTbl WHERE id=$ctx.args.updateQuestionTblInput.id"]
}