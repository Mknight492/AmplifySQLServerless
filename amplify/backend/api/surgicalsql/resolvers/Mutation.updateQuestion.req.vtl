#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateQuestionInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateQuestionInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Question SET $update WHERE id=$ctx.args.updateQuestionInput.id", "SELECT * FROM Question WHERE id=$ctx.args.updateQuestionInput.id"]
}